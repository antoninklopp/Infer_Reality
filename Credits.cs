using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LanguageModule;
using UnityEngine.UI; 

public class Credits : MonoBehaviour {

	// Use this for initialization
	void Start () {
  //      GameObject TextObj = GameObject.Find("CreditsText");
  //      string all = "";
		//for (int i = 1; i <= 13; i++) {
  //          all += LanguageData.GetString("string" + i.ToString(), "credits") + "\n"; 
  //      }
  //      TextObj.GetComponent<Text>().text = all; 
	}
}
